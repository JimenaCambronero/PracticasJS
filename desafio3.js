/*
let nombre;
let costoDiarioGato = 15
let costoDiarioPerro = 20

function costoPerro (periodo,costoDiarioPerro,cantidadMascotas) {
   return ((periodo * costoDiarioPerro) * cantidadMascotas);
}

function costoGato (periodo,costoDiarioGato,cantidadMascotas) {
   return ((periodo * costoDiarioGato) * cantidadMascotas);
}

nombre = prompt ("Introduce tu nombre");
    alert ("Bienvenida/o " + nombre);

    alert ("hagamos ejercicios con funciones y calculemos el costo del cuidado de tu mascota");

let cantidadMascotas = prompt ("Ingresa cuantas mascotas quieres que cuidemos!");
    
if (cantidadMascotas >=1 && cantidadMascotas <=3){
    alert ("Podemos cuidar a tu/s mascota/s")

}else {  
    alert ("No podemos cuidar a tus mascotas");
}

let mascota = prompt ("Indicanos si tu mascota es perro o gato");
let periodo = parseInt (prompt ("Cuantos dias quieres que cuidemos a tu " + mascota));
         
if (mascota === "perro"){
    
    alert ("El costo total por el cuidado de tu " + mascota + " es de " + costoPerro (periodo,costoDiarioPerro,cantidadMascotas)+ " euros");
}

else if (mascota === "gato"){

    alert ("El costo total por el cuidado de tu  " + mascota + " es de " + costoGato (periodo,costoDiarioPerro,cantidadMascotas)+ " euros");
}   
else {
    alert("Contactate por e-mail para consultar");
    }   
 */   